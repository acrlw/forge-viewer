"""WebGPU (wgpu-py) render backend: offscreen color/depth/segmentation rendering.

Import ``forge_viewer.render.webgpu.backend`` directly; the package root stays
import-free so ``wgpu`` is only required when the backend is actually selected.
"""
