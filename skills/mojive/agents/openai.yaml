interface:
  display_name: "Mojive"
  short_description: "Inspect, edit, and verify Mojive 3D scenes"
  default_prompt: "Use $mojive to inspect the active scene, apply the requested changes, and verify the result."
